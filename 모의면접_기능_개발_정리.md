# JobPilot AI - "AI모의면접" 기능 개발 정리 (면접 설명용)

## 1. 기능 개요

JobPilot AI는 구직자를 위한 취업 준비 플랫폼이고, 그 중 "AI모의면접"은 사용자가 카메라/마이크 또는 텍스트 채팅으로 실전처럼 면접을 연습하고, AI가 답변을 분석해서 100점 만점 리포트(강점/개선점/모범답안)를 주는 기능입니다.

**기술 스택**
- 백엔드: Spring Boot 3.4 (Java 21) — 회원/경력프로필/채용공고 등 도메인 로직, MySQL(Flyway 마이그레이션)
- AI 서버: Python FastAPI — 질문 생성, STT(음성인식), 음성/얼굴 지표 분석, TTS, Gemini 기반 평가/리포트
- 프론트: React + TypeScript (Vite)
- 배포: AWS EC2, Docker, Nginx

프론트는 Spring 백엔드와 별개로 AI 서버(FastAPI, 포트 8001)에 직접 요청을 보내는데, Vite dev 서버의 `/ai-api` 프록시를 거쳐서 CORS 문제 없이 통신하도록 구성했습니다.

> **작업 범위 참고**: 이번에 제가 직접 코드를 작성한 부분은 **AI 서버(Python/FastAPI)**와 **프론트엔드(React)** 쪽입니다. 스프링(Java) 백엔드는 직접 건드리지 않았고, 이미 존재하던 회원 경력프로필 API(`GET /api/v1/members/me/career-profile`)를 프론트에서 호출만 했습니다. 면접에서 "스프링 쪽도 작업했냐"고 물어보면, 이번 기능에서는 AI 서버/프론트 위주로 작업했고 스프링 엔드포인트는 기존에 있던 걸 소비만 했다고 정확히 답하는 게 좋습니다.

---

## 2. 핵심 작업 1 — 개인 맞춤형 질문 생성 (가장 중요한 기술 의사결정)

**배경/문제**
기존에는 `EleutherAI/polyglot-ko-1.3b`를 LoRA로 파인튜닝한 모델이 질문을 생성하고 있었습니다. 사용자의 회원가입 시 입력한 경력 프로필(목표 직무, 기술 요약)을 반영해서 "맞춤형 질문"을 내려는 요구사항이 있었는데, 구현 도중 **LoRA 모델의 학습 데이터가 전부 "ICT/신입" 카테고리로만 구성돼 있어서, `job`/`context` 파라미터를 아무리 바꿔도 실제 생성되는 질문에 영향이 거의 없다는 걸 모델 코드의 docstring을 통해 직접 확인**했습니다. 즉 기존 모델은 구조적으로 "맞춤형"이 불가능했습니다.

**의사결정**
모델을 처음부터 재학습하기엔 시간이 부족했기 때문에, 기존 LoRA 경로는 그대로 두고(기본값 폴백용), **Gemini API를 활용한 별도 경로를 신설**해서 사용자 정보(목표 직무, 기술 요약, 카테고리)가 있을 때만 그 경로로 라우팅하는 **이원화 아키텍처**로 설계했습니다.

**구현**
- `question_generator.py`에 `generate_personalized_question(job, tech_summary, category)` 함수 추가 — Gemini에 프롬프트를 구성해서 보내고, 응답을 정제(따옴표 제거, 길이 제한, 존댓말 정규화)해서 반환
- `router.py`의 `NextQuestionRequest`에 `wants_personalized()` 메서드를 둬서, "job이 기본값이 아니거나 tech_summary가 있으면" Gemini 경로, 아니면 기존 LoRA 경로를 타도록 분기
- 실패 시(Gemini 키 없음, API 에러, 빈 응답 등) `None`을 반환해서 자동으로 LoRA 경로로 폴백 — **fail-open 설계**로 어떤 상황에서도 질문 생성 자체가 죽지 않도록 함
- pytest로 유닛테스트 작성 (`test_question_generator.py`, `test_router_next_question.py`) — Gemini 클라이언트를 mock해서 성공/실패/빈 응답 케이스 검증

**면접에서 강조할 포인트**
- 기존 모델(구현된 기능)의 한계를 코드/문서를 직접 확인해서 발견하고, 무리하게 기존 구조를 고치기보다 "신규 경로 + 안전한 폴백"으로 리스크를 낮춘 설계 판단
- 하나의 엔드포인트가 두 개의 서로 다른 생성 전략(로컬 파인튜닝 모델 vs 외부 LLM API)을 조건에 따라 라우팅하는 구조 — 확장성 고려(나중에 세 번째 전략을 추가해도 라우팅 로직만 확장하면 됨)

---

## 3. 핵심 작업 2 — 분야별 질문 분류

사용자가 시작 화면에서 백엔드/프론트엔드/풀스택/모바일/데이터AI 중 하나를 고르면, 그 값이 `job` 파라미터로 들어가서 위의 `wants_personalized()` 조건을 만족시켜 Gemini 맞춤 질문 경로를 타게 됩니다. 이미 있던 채용공고 검색 쪽의 9개 카테고리(`JobPostingSearchService.ROLE_KEYWORDS`)를 그대로 재사용하지 않고, 모의면접에 맞게 5개로 단순화한 별도 목록을 만들었습니다 — Gemini 프롬프트는 자유 텍스트라 채용공고 필터링용 enum과 1:1로 맞출 필요가 없다고 판단했기 때문입니다.

---

## 4. 핵심 작업 3 — TTS(질문 음성 안내) 다양화

Google Cloud TTS를 쓰고 있었는데, 사용자가 "목소리가 다 비슷하게 들린다"고 피드백을 줬습니다. 원인을 조사해보니 Standard/Neural2 티어가 같은 알파벳(A/B/C...)이면 같은 성우 목소리를 쓰고, 합성 품질만 다른 구조였습니다. Google의 최신 **Chirp3 HD** 티어(8개 코어 페르소나: Aoede/Kore/Leda/Zephyr 여성, Puck/Charon/Fenrir/Orus 남성)가 더 감정 표현이 풍부하고 "캐릭터"처럼 들리는 걸 확인해서, 그 중 4개를 추가로 붙였습니다(무료 할당량은 Neural2와 동일하게 월 100만자). 기존 `VoiceOption` 리스트에 항목만 추가하면 되는 구조라 백엔드 로직 변경 없이 확장했습니다.

---

## 5. 핵심 작업 4 — 채팅형(텍스트) 면접 연습 모드 전면 개편

기존엔 화면 우측 하단에 작은 플로팅 위젯으로 채팅 면접을 열었는데, "질문마다 시간 압박이 없어서 실전감이 떨어진다"는 피드백으로:
- 카메라 모드처럼 **전체 화면**으로 전환 (기존에 있던 `stage:"typing"` 흐름을 재사용해서 device-check 단계를 건너뛰는 새 진입점 `startChatModeSession()` 추가)
- **90초 게이지 바 타이머** 추가 — 파랑→주황→빨강으로 색이 바뀌는 프로그레스 바, 시간 종료 시 입력된 답변을 자동 제출
- React의 `useEffect` 3개로 (1)질문 바뀔 때 타이머 리셋 (2)매초 카운트다운 (3)0초 도달 시 자동 제출을 구현 — 기존 코드베이스에 이미 있던 `break`/`countdown` 타이머 패턴을 그대로 재사용해서 일관성을 유지

---

## 6. 핵심 작업 5 — 모의면접 시작 화면 UX 반복 설계

사용자(실사용자 관점) 피드백을 여러 차례 받으며 반복 개선:
드롭다운 → 라디오 칩 → 카드형 2개(카메라/채팅) + 칩(분야/질문개수/목소리) → 카드를 클릭 가능한 라디오처럼 동작하게 바꾸되 시각적 카드 형태는 유지, 선택 시 배경이 칩과 동일한 파란색으로 바뀌는 `.active` 상태 추가, 실제 시작 버튼은 카드 밖 하단에 별도 배치.

**포인트**: 디자인 결정이 한 번에 확정되지 않고 실사용자 피드백 기반으로 여러 번 되돌리고 다시 다듬는 과정을 거쳤다는 것 — 실무에서 흔한 반복 개발/애자일적 태도를 보여줄 수 있는 사례.

---

## 7. Git / 협업 워크플로우 경험

팀 프로젝트라 `main`(배포 기준 브랜치)과 개인 작업 브랜치(`깅다링`)를 분리해서 작업했고, 이번에 다음 상황을 실제로 겪고 해결했습니다:

- **PR(Pull Request) 생성 시 "Can't automatically merge"** 경고 — GitHub이 두 브랜치의 diff를 자동 병합할 수 없다고 판단한 상황
- 로컬(IntelliJ)에서 먼저 `main`에 내 브랜치를 merge 시도 → **`AppShell.tsx`, `styles.css` 두 파일에서 충돌** 발생 (팀원이 같은 파일의 다른 부분을 동시에 수정)
  - `AppShell.tsx`: 팀원이 추가한 "카카오 스크립트 로딩 강제 렌더링 훅"과 내가 추가한 "채팅 위젯 Provider로 감싸기"가 같은 위치(return문)를 건드려서 충돌 → 두 로직을 모두 살리는 방향으로 수동 병합
  - `styles.css`: 서로 완전히 다른 CSS 블록을 파일 끝에 각자 추가해서 발생한 충돌 → 실제로는 겹치는 스타일이 없어서 두 블록을 그대로 이어붙이는 방식으로 해결
- 충돌 해결 후 로컬 `main`을 push해서 원격 `main`에 반영 — GitHub PR은 별도로 머지 버튼을 누르지 않아도 커밋이 이미 `main`에 들어갔기 때문에 자동으로 반영됨

**면접에서 강조할 포인트**: 단순히 "Accept Yours/Theirs"로 한쪽을 버리지 않고, 두 변경 사항의 의도를 파악해서 로직을 합치는 방식으로 충돌을 해결했다는 점 — 코드 리뷰/협업 능력을 보여줄 수 있는 경험.

---

## 8. 예상 면접 질문 & 답변 방향

**Q. 왜 질문 생성에 LoRA와 Gemini 두 가지를 같이 쓰나요?**
A. 로컬 파인튜닝 모델은 응답 속도/비용 면에서 유리하지만 학습 데이터의 한계로 개인화가 안 됐고, Gemini는 외부 API라 비용/지연이 있지만 프롬프트만으로 유연하게 개인화가 가능했습니다. 사용자가 정보를 제공했을 때만 Gemini를 타고, 아니면 기존 로컬 모델로 폴백하게 해서 두 방식의 장점을 조합했습니다.

**Q. Gemini 응답이 이상하게 오면 어떻게 처리하나요?**
A. try/except로 감싸고, 응답이 비었거나 너무 길면(150자 초과) `None`을 반환해서 상위 라우터가 자동으로 기존 LoRA 경로로 폴백하도록 설계했습니다. 사용자 입장에서는 어떤 경우에도 질문 생성 자체가 실패하지 않습니다.

**Q. 프론트/백엔드 간 개인정보(경력 프로필)는 어떻게 전달되나요?**
A. 프론트가 **이미 존재하던** 스프링 백엔드의 `/api/v1/members/me/career-profile`을 호출해서 목표 직무/기술 요약을 가져오고(이 엔드포인트 자체는 이번에 만든 게 아니라 기존 회원 프로필 기능을 그대로 재사용한 것), 그 값을 AI 서버의 `/next-question` 요청 바디에 실어서 보냅니다. AI 서버는 회원 DB에 직접 접근하지 않고, 매 요청마다 필요한 값만 프론트로부터 전달받는 구조라 AI 서버와 회원 DB가 결합되지 않습니다.

**Q. 이번 작업에서 스프링(백엔드) 쪽도 직접 구현했나요?**
A. 아니요, 스프링 코드는 직접 작성하지 않았습니다. 이번 기능(개인 맞춤 질문 생성, TTS 확장, 시작화면 UI 개편, 채팅 모드 개편)은 AI 서버(FastAPI/Python)와 프론트(React) 영역이었고, 스프링 쪽 회원 경력프로필 API는 기존에 이미 구현돼 있던 걸 그대로 호출해서 재사용했습니다.

**Q. Git 충돌은 어떻게 해결했나요?**
A. 두 변경이 같은 파일의 다른 목적의 코드를 건드린 상황이었어서, 어느 한쪽을 버리는 대신 두 로직의 의도를 파악해서 수동으로 합쳤습니다. CSS 쪽은 겹치는 게 없는 걸 확인하고 단순히 이어붙였습니다.
