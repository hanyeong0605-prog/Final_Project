server:
  servlet:
    session:
      cookie:
        # Share the temporary OAuth authorization session between the apex and www hosts.
        domain: job-a-dream.site
        secure: true
        same-site: lax
