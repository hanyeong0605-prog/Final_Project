import { FormEvent,useEffect,useState } from "react";import { ArrowLeft,MessageCircle,Send,ThumbsUp } from "lucide-react";import { Link,useNavigate,useParams } from "react-router-dom";import * as api from "../features/community/api/communityApi";import type { Comment,Post } from "../features/community/api/communityApi";
export function CommunityDetailPage(){const{id}=useParams(),navigate=useNavigate(),postId=Number(id);const[post,setPost]=useState<Post|null>(null),[comments,setComments]=useState<Comment[]>([]),[body,setBody]=useState(""),[editing,setEditing]=useState(false),[title,setTitle]=useState(""),[editedBody,setEditedBody]=useState(""),[deleting,setDeleting]=useState(false),[error,setError]=useState("");const load=()=>{void api.detail(postId).then(item=>{setPost(item);setTitle(item.title);setEditedBody(item.body)}).catch(e=>setError(e instanceof Error?e.message:"게시글을 불러오지 못했습니다."));void api.comments(postId).then(setComments)};useEffect(load,[postId]);const submitComment=async(e:FormEvent)=>{e.preventDefault();if(!body.trim())return;await api.comment(postId,body);setBody("");setComments(await api.comments(postId));};if(error)return <section className="community-detail-page"><Link className="community-back-link" to="/community"><ArrowLeft size={16}/>목록으로</Link><p className="form-error">{error}</p></section>;if(!post)return <section className="community-detail-page">게시글을 불러오는 중입니다.</section>;return <section className="community-detail-page"><Link className="community-back-link" to="/community"><ArrowLeft size={16}/>목록으로</Link>{editing?<form className="community-editor" onSubmit={async e=>{e.preventDefault();const saved=await api.update(post.id,{boardType:post.boardType,title,body:editedBody,serviceFeedback:post.serviceFeedback,privatePost:post.privatePost});setPost(saved);setEditing(false)}}><label>제목<input value={title} onChange={e=>setTitle(e.target.value)} required maxLength={200}/></label><label>내용<textarea value={editedBody} onChange={e=>setEditedBody(e.target.value)} required maxLength={5000}/></label><div className="form-actions"><button className="outline-button" type="button" onClick={()=>setEditing(false)}>취소</button><button className="primary-button">수정 저장</button></div></form>:<article className="community-post-detail"><span className="community-post-category">{post.boardType==="FREE"?"자유게시판":"Q&A 게시판"}</span><h1>{post.title}</h1><small>{post.author} · {new Date(post.createdAt).toLocaleString("ko-KR")}</small><p>{post.body}</p><div className="community-detail-actions"><button onClick={async()=>{await api.like(post.id);load()}}><ThumbsUp size={15}/>좋아요 {post.likes}</button>{post.mine&&<><button onClick={()=>setEditing(true)}>수정</button><button className="danger" onClick={()=>setDeleting(true)}>삭제</button></>}</div>{deleting&&<div className="community-inline-confirm"><span>이 글을 삭제할까요?</span><button className="outline-button" onClick={()=>setDeleting(false)}>취소</button><button className="danger" onClick={async()=>{await api.remove(post.id);navigate("/community")}}>삭제</button></div>}</article>}<section className="community-comments"><h2><MessageCircle size={18}/>댓글 {comments.length}</h2>{comments.map(item=><article key={item.id}><strong>{item.author}</strong><small>{new Date(item.createdAt).toLocaleString("ko-KR")}</small><p>{item.body}</p><button onClick={async()=>{await api.likeComment(item.id);setComments(await api.comments(postId))}}><ThumbsUp size={13}/> {item.likes}</button></article>)}<form className="community-comment-editor" onSubmit={submitComment}><textarea value={body} onChange={e=>setBody(e.target.value)} maxLength={2000} placeholder="댓글을 입력하세요"/><button className="primary-button"><Send size={14}/>등록</button></form></section></section>}
