import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { ExternalLink } from "lucide-react";
import { getOpportunity } from "../features/opportunities/api/opportunitiesApi";
import type { Opportunity } from "../features/opportunities/model/opportunity.types";
import { DataStatePanel } from "../shared/components/DataStatePanel";
import { PageHeading } from "../shared/components/PageHeading";
export function OpportunityDetailPage() { const { id="" }=useParams(); const [item,setItem]=useState<Opportunity>(); const [error,setError]=useState(false); useEffect(()=>{void getOpportunity(id).then(setItem).catch(()=>setError(true));},[id]); if(error)return <DataStatePanel state="error"/>; if(!item)return <DataStatePanel state="loading"/>; const money=(v:number|null)=>v===null?"정보 없음":`${v.toLocaleString()}원`; return <><PageHeading eyebrow="WORK24 TRAINING" title={item.title} body={`${item.organization} · 훈련 기간 ${item.period}`} /><section className="panel"><span className={`type-badge ${item.status === "EXPIRED" ? "orange" : "blue"}`}>{item.status === "EXPIRED" ? "훈련 종료" : "훈련 진행/예정"}</span><h2>{item.title}</h2><div className="skills">{item.tags.map(tag=><span key={tag}>{tag}</span>)}</div><div className="spec-summary"><div><span>훈련기관</span><strong>{item.organization}</strong></div><div><span>훈련 지역</span><strong>{item.address || "정보 없음"}</strong></div><div><span>훈련 대상</span><strong>{item.trainingTarget || "정보 없음"}</strong></div><div><span>문의</span><strong>{item.phone || "정보 없음"}</strong></div><div><span>정원 / 신청</span><strong>{item.capacity ?? "-"}명 / {item.enrolledCount ?? "-"}명</strong></div><div><span>총 수강료 / 실제 부담금</span><strong>{money(item.courseFee)} / {money(item.selfPayFee)}</strong></div><div><span>만족도</span><strong>{item.satisfactionScore ?? "정보 없음"}</strong></div></div><div className="form-actions"><Link className="outline-button" to="/opportunities">목록으로</Link><a className="primary-button" href={item.detailUrl || item.sourceUrl} target="_blank" rel="noreferrer">고용24에서 확인 <ExternalLink size={15}/></a></div></section></>; }
