import { useEffect,useState } from "react";import { Link } from "react-router-dom";import { getJson } from "../../../api/httpClient";
type Rank={id:number;name:string;title?:string;reviewCount:number;averageRating:number;positiveSentiment?:number;score:number};
export function HomeReviewRankings(){const [tab,setTab]=useState<"companies"|"postings">("companies"),[rows,setRows]=useState<Rank[]>([]);useEffect(()=>{getJson<Rank[]>(`/api/v1/review-companies/rankings/${tab}`).then(setRows).catch(()=>setRows([]))},[tab]);return <section className="panel home-review-ranking" data-scroll-reveal><div className="home-section-heading"><div><span className="eyebrow">DEMO REVIEW RANKING</span><h2>사용자 평가가 좋은 {tab==="companies"?"가상기업":"가상공고"} TOP 10</h2></div><Link to="/company-reviews">전체 리뷰 보기</Link></div><div className="ranking-tabs"><button className={tab==="companies"?"active":""} onClick={()=>setTab("companies")}>기업</button><button className={tab==="postings"?"active":""} onClick={()=>setTab("postings")}>공고</button></div>{rows.length?<ol>{rows.map((r,i)=><li key={r.id}><b>{i+1}</b><span><strong>{r.title||r.name}</strong><small>{r.title&&r.name} · 리뷰 {r.reviewCount}개 · 평균 {r.averageRating.toFixed(1)}점</small></span><em>{r.score.toFixed(3)}</em></li>)}</ol>:<p className="data-empty">분석 완료 후 순위가 표시됩니다.</p>}<small>가상 시연 데이터의 별점·감정분석·표본 수·최신성을 보정한 참고 순위입니다.</small></section>}
